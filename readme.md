В задаче сказано:

> Данные отсортировать по возрастанию коэффициента.

Но там много коэффицентов и я не понял по какому именно сортировать надо, да и дат много же будет на одном листе, как будто все будет в перемешку, либо я чего то не понял, было бы круто обсудить на собесе это.
Поэтому я позволил себе внести изменения в условия и просто размещаю на одном листе данные за один год, вверху распологаю более поздние даты тарифов и соответсвено данные коэфицентов этих тарифов, а ниже тарифы с более ранней датй, то есть сортирую по дате, от большей к меньшей😀

Перед запуском приложения, надо файл example.env переименовать в .env. И добавить в этот файл ключ API_KEY_WB. Я не стал указывать тот что вы мне дали, все таки реп публичный, а это ваш ключ. Так же есть возможность настроить частоту обновления данных, по умолчанию я оставил 1 час. Частота обновления задается cron паттерном. А под капотом это выражение обрабатывает bullMQ.

Для удобной проверки, лучше сразу перейти на гугл таблицу(она будет пустая изначально), что бы увидеть как позже на ней появляются данные☺️
Гугл таблица доступна по адресу:

> https://docs.google.com/spreadsheets/d/1i4Td_JYRuM7pr42aWjQsQ6LMyanNUrOXFd3B3Ij8oKk/edit?gid=2040628768#gid=2040628768

Теперь вы готовы к запуску!🚀

Для запуска достаточно прописать в терминале команду:

```bash
docker-compose up -d
```

Далее в контейнере приложения `app` можно смотреть логи, а так же в саму таблицу.
